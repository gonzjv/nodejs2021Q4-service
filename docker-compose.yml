# version: '1.29.2'
services:
  node:
    build: .
    ports:
      - '4000:4000'
    depends_on:
      - postgres
    environment:
      - PORT=4000
  postgres:
    build: ./database
    ports:
      - '5000:5000'
    environment:
      POSTGRES_PASSWORD: 'password'
